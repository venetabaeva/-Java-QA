/**
 * 
 */
package javaW3Schools;

/**
 * @author venetabaeva
 *
 */
public class Java_0037 {

	/**
	 * @param args
	 */
	public static void main(String[] args) {
		// TODO Auto-generated method stub
		//Package
				/*
				 * used to group related classes
				 * as a folder in a file directory
				 * packages to avoid name conflicts, and to write a better maintainable code
				 * to create a package, use the package keyword
				 */
		
		//Example:
		       /**
		        * package mypack;
		        * class MyPackageClass {}
		        * public static void main(String[] args) 
		        * System.out.println("This is my package!");
		        */
		
		//Types
				/*
				 * Built-in Packages (packages from the Java API)
				 * User-defined Packages (create  packages)
				 */
		//Java API is a library of pre - written classes, that are free to use, included in the Java Development Environment
				/*
				 * https://docs.oracle.com/javase/8/docs/api/ -> complete list 
				 */
		//Library
				/*
				 * is divided into packages and classes
				 * either import a single class (along with its methods and attributes), or a whole package that contains all the classes that belong to the specified package
				 * to use a class or a package from the library, one needs to use the import keyword
				 */
			
				    
	}

}
